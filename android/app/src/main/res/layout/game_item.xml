<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#252525">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- Ícone do Jogo (Esquerda) -->
        <ImageView
            android:id="@+id/gameImage"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/controller"
            android:background="@drawable/box_gradient"
            android:padding="8dp"
            android:scaleType="centerInside"
            app:tint="#FF5722" 
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Título do Jogo (Centro) -->
        <TextView
            android:id="@+id/gameName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            tools:text="Super Mario Bros 3"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/gameImage"
            app:layout_constraintEnd_toStartOf="@id/magicButton"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/gameDetails"/>

        <!-- Detalhes extras (Abaixo do título) -->
        <TextView
            android:id="@+id/gameDetails"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="NES System"
            android:textColor="#AAAAAA"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="@id/gameName"
            app:layout_constraintEnd_toEndOf="@id/gameName"
            app:layout_constraintTop_toBottomOf="@id/gameName"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Botão Magic/Genie (Opcional) -->
        <ImageButton
            android:id="@+id/magicButton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/magic"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="#FFD700"
            android:visibility="gone"
            app:layout_constraintEnd_toStartOf="@id/playButton"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Botão Jogar (Direita) -->
        <Button
            android:id="@+id/playButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="JOGAR"
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#E64A19"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
